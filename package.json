{
  "type": "module",
  "dependencies": {
    "@fastly/js-compute": "^3.46.0"
  },
  "scripts": {
    "build": "aio aem edge-functions build",
    "deploy": "aio aem edge-functions deploy",
    "test": "mocha"
  },
  "devDependencies": {
    "mocha": "12.0.0-beta-9.2"
  },
  "overrides": {
    "esbuild": ">=0.28.1",
    "@bytecodealliance/weval": "^0.5.0"
  }
}
